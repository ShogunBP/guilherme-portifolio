- **HTML Injection fix**: Replace flawed angle-bracket stripping (`.replace(/[<>]/g, '')`) with proper HTML encoding. When substituting user-provided text into a context interpreted as HTML (e.g. HTML emails via Resend), map `&`, `<`, `>`, `"`, and `'` to their respective HTML entities using a regex `.replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;").replace(/"/g, "&quot;").replace(/'/g, "&#039;")`.
